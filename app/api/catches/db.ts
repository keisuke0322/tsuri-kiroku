import { env } from 'cloudflare:workers';
export function getDb(){const db=(env as unknown as {DB?:D1Database}).DB;if(!db)throw Error('Database unavailable');return db}
export function parseCatch(x:unknown){if(!x||typeof x!=='object')return null;const v=x as Record<string,unknown>;const date=String(v.date||''),location=String(v.location||'').trim(),species=String(v.species||'').trim(),count=Number(v.count),length=v.length===''||v.length==null?null:Number(v.length),method=String(v.method||'').trim(),memo=String(v.memo||'').trim();if(!/^\d{4}-\d{2}-\d{2}$/.test(date)||Number.isNaN(Date.parse(date))||!location||location.length>120||!species||species.length>80||!Number.isInteger(count)||count<1||count>9999||length!==null&&(!Number.isFinite(length)||length<0||length>999)||method.length>200||memo.length>2000)return null;return {date,location,species,count,length,method,memo}}
export function fail(e:unknown){console.error('Catch storage error',e);return Response.json({error:'記録を保存・読み込みできませんでした。再試行してください。'},{status:500})}
